fn main(a:int, b:A)->A{
    let ak:int = 0;
    if ((ak > 1)){
        ret ak + 1;
    }else{
        ret ak -1 ;
        break;
    }
    let aa: A;
    let bb:B ;
    let k = 10;
    let pp:A;
    pp = 10;
    aa.test = 10;
    bb.a = aa;
    let t[100]:int = {} ; 
    t[1] = 5;
    t[a] = t[1] + a*3*aa;
    while((b > 0 || (b>5 && a !=20) && a!=b) && a <= 900){
        if(b>0){
            if(c > 0){
                if(20 > 9){
                    break;
                }else{
                    continue;
                }
            }
        }else{
            a = b*2/90;
        }
        continue;
    }


    ret ak;
}

struct A{
    test:int
}

struct B{
    test: int,
    a:A,
    t:int
}